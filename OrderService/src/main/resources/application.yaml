server:
  port: 8074

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myuser
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: OrderService
  mail:
    host: smtp.gmail.com
    port: 587
    username: sahilkaul52@gmail.com
    password: muve jaiu jxsf xsej
    properties:
      mail:
        smtp:
          auth:
            starttls:
              enable: true

